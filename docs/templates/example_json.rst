Example JSON
============

The example JSON is used as an example for the AI how to format the json. As there is no
convention how to represent a database as JSON, this is necessary. Otherwise the representation may change
from AI to AI and even from call to call.

.. literalinclude:: /../db_generation/Example.json
    :language: json
    :linenos: